# API Base URL for backend requests
# IMPORTANT: In production, change this to your deployed backend API URL (e.g., https://api.yourdomain.com/api)
# Note: Vite bundles these variables at build-time. Set this BEFORE running `npm run build`!
VITE_API_URL=http://localhost:8080/api
